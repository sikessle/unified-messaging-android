package de.htwg.tqm.app.model;

/**
 * Only needed to process different kinds of expandable list view detail objects equally
 * (inbox detail, issue quality metric detail, issue count metric detail)
 */
public interface DashboardDetail { }
